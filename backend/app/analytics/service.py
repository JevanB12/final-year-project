"""Analytics recording (stub)."""

from typing import Any, Dict, Optional


def record_event(name: str, payload: Optional[Dict[str, Any]] = None) -> None:
    """No-op until analytics pipeline exists."""
    _ = (name, payload)
